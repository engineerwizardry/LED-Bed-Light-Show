//Code for the LED BED Light Show 
//A shift register will not be used in this project due to its unnecessary complexity. 

const int led1 = 1;
const int led2 = 2;
const int led3 = 3;
const int led4 = 4;
const int led5 = 5;
const int led6 = 6;
const int led7 = 7;
const int led8 = 8; 
const int led9 = 9;
const int led10 = 10;
const int led11 = 11;
const int led12 = 12;
const int led13 = 13; 

boolean lastbutton = LOW;
boolean currentbutton = LOW;
int ledPattern = 0; 

void setup() {
    pinMode(led1, OUPUT);
    pinMode(led2, OUTPUT);
    pinMode(led3, OUTPUT);
    pinMode(led4, OUTPUT);
    pinMode(led5, OUTPUT);
    pinMode(led6, OUTPUT);
    pinMode(led7, OUTPUT);
    pinMode(led8, OUTPUT);
    pinMode(led9, OUTPUT);
    pinMode(led10, OUTPUT);
    pinMode(led11, OUTPUT);
    pinMode(led12, OUTPUT);
    pinMode(led13, OUTPUT);
};

void ledPatternNum (int pat) {
   if (pat == 1) {
      digitalWrite(
      
      
      
void loop() {};



